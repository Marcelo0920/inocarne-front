# URL base de la API.
# En desarrollo se deja vacía: Vite redirige /api a http://localhost:4000.
# En producción se apunta al backend desplegado, por ejemplo:
#   VITE_API_URL=https://inocarne-api.onrender.com
VITE_API_URL=
